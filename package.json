{
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && nitro build",
    "preview": "npx srvx --prod .output/ --host 0.0.0.0 --port 4000"
  },
  "devDependencies": {
    "nitro": "latest"
  }
}
